{
  "name": "manuscript-checker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy",
    "dev": "uv run pywrangler dev",
    "start": "uv run pywrangler dev",
    "build": "uv run pywrangler build"
  },
  "devDependencies": {
    "wrangler": "^4.108.0"
  }
}